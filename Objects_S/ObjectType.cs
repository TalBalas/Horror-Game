using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectType : MonoBehaviour
{
 /*  public enum ObjectsType {Jerrican,SquareWood};
   public ObjectsType ObjectTypes;
   [SerializeField] HoldObject holdObject;
    void OnTriggerStay(Collider other)
    {
      
        if (other.gameObject.CompareTag("Player"))
        {
            switch (ObjectTypes)
            {
                case ObjectsType.Jerrican:
                    holdObject.HoldAndDropJerrican();
               
                    break;
                case ObjectsType.SquareWood:
                    holdObject.HoldAndDropQuareWood();
                  
                    break;
                default:
                    break;
            }
        }
    }*/

}
